# Server
PORT=3000

# MySQL Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=nova

# CORS — comma-separated allowed origins (frontend URLs)
# CORS_ORIGIN=https://nova-esiea.vercel.app,http://localhost:5173

# IoT Telemetry DB (séparée de nova)
DB_TELEMETRY_NAME=nova_telemetry

# API key pour l'ESP32 (header x-api-key)
# Laisser vide en dev pour désactiver la vérification
TELEMETRY_API_KEY=change_me_in_production
