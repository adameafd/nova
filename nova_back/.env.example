# Server
PORT=3000

# MySQL Database (local)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=nova

# CORS — origines autorisées (frontend URLs, séparées par virgule)
# CORS_ORIGIN=http://localhost:5173

# IoT Telemetry DB (créée automatiquement au démarrage)
DB_TELEMETRY_NAME=nova_telemetry

# API key pour l'ESP32 (header x-api-key)
# Laisser vide en dev pour désactiver la vérification
TELEMETRY_API_KEY=
