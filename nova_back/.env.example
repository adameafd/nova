# Server
PORT=3000

# PostgreSQL / Supabase — connection string complète
# Supabase → Settings → Database → Connection string → URI (Transaction pooler)
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-eu-west-3.pooler.supabase.com:6543/postgres

# CORS — origines autorisées (URLs frontend, séparées par virgule)
# CORS_ORIGIN=https://nova-react.vercel.app,http://localhost:5173

# IoT Telemetry DB (même connexion Supabase, base différente)
DB_TELEMETRY_NAME=nova_telemetry

# API key pour l'ESP32 (header x-api-key)
# Laisser vide en dev pour désactiver la vérification
TELEMETRY_API_KEY=change_me_in_production
