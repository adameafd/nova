# Server
PORT=3000

# MySQL Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=nova

# CORS — comma-separated allowed origins (frontend URLs)
# CORS_ORIGIN=https://nova-esiea.vercel.app,http://localhost:5173

# MQTT Broker (for IoT sensor data)
# MQTT_BROKER_URL=mqtt://localhost:1883
# MQTT_TOPIC=nova/sensors/#
