{
  "installCommand": "cd nova-react && npm install",
  "buildCommand": "cd nova-react && npm run build",
  "outputDirectory": "nova-react/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
